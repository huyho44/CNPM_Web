// Legacy constants kept for compatibility — main data is now in src/mock/data.ts
export { ZONE_NAMES as ZONES, MOCK_USERS, INITIAL_SLOTS as MOCK_SLOTS, INITIAL_POLICIES as MOCK_POLICIES } from './mock/data';
